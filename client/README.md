# Gmail Cleaning Tool Frontend

Simple React interface for searching and managing Gmail messages.

## Setup

```bash
cd client
npm install
npm start
```

The app proxies API requests to the Flask server running on port 5000.
